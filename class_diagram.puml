@startuml
' Set direction
left to right direction

' Define classes
class App {
  +run()
  +handle_analysis_click()
  +display_results(score_data, graph_html)
}

class LLMEngine {
  -client: OpenAI
  +analyze_contradiction(claim: str, evidence: str) dict
  -build_prompt(claim, evidence) str
}

class GraphVisualizer {
  +generate_risk_map(target_company: str, risk_score: float) str
  -build_graph(data) : Graph
  -simulate_contagion(score) : dict
  -style_nodes(graph) : Graph
  -save_to_html(net) : str
}

' External Libraries
class OpenAI <<External Library>> {
  +chat.completions.create()
}

class NetworkX <<External Library>> {
  +Graph()
  +add_node()
  +add_edge()
}

class Pyvis <<External Library>> {
  +Network()
  +from_nx()
  +show()
}

' Define relationships
App ..> LLMEngine : "uses 1"
App ..> GraphVisualizer : "uses 1"
LLMEngine ..> OpenAI : "uses 1"
GraphVisualizer ..> NetworkX : "uses"
GraphVisualizer ..> Pyvis : "uses"

@enduml